# Netlify configuration for Vite React app
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  GIT_LFS_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

